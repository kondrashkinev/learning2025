Power Shell

1. git --version
2. git config --global user.name <..>   //используем глобальные конфигурации
3. git config --global user.email <..>  //используем глобальные конфигурации 
  3.1 git config user.email <..>   //м. исп-ть локальные конфиги для имени и почты, они будут применены в той дирректории, в которой находимся.
4. git config --list
  4.1gitk   //редактор, но запускать, только из инициализированной дирректории

5. echo "Some text" > file.txt   //создание file.txt с текстом "Some text"
6. cat file.txt                  //просмотр файла
7. rm file.txt                  //удаление файла
8. ls -Force                  //просмотр папки

9. git init        				//создание нового git репозитория
10. git branch -m <name>  		//новое имя для текущей ветки

11. git status    //вывод состояния текущего репозитория
 11.1 если выводит красным, значит надо сделать add
 11.2 если зелёным надо закомитить, лучше с комментарием
 11.3 д.б. nothing to comit

12. git add .    		//добавляет все файлы (которые создали в раб. дирректории) в индекс 
 12.1 git add *    		//добавляет все файлы (которые git видит, но не знает о них ничего) в индекс 
 12.2 git add test.txt    //добавляет файл test.txt в индекс
13. git commit      // сохраняет commit и указывает на текущую версию проекта (сохраняет файлы в репозиторий) #сохранить
 13.1 git commit -m "Comment Comit"      
14. git checkout      //перейти к определённой версии проекта (гит берет файлы из репозитория и перемещает их в раб. дирректорию, поэтому раб. дирректория перезаписывается)


17. git reset      //вернуть состояние всей ветки, файла, комита
	17.1 git reset HEAD filename    //HEAD то последнее состояние в к-ом был файл (ветки, комита), до изменений
	17.2 git checkout filename   // обязательно сделать эту команду, чтобы не сохранять сделанные изменения
 


